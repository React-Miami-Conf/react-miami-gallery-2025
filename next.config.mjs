/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'basehub.earth',
            'assets.basehub.com',
        ]
    }
};

export default nextConfig;
